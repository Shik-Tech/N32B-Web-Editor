const OUTPUT_TRS = 0;
const OUTPUT_USB = 1;
const OUTPUT_BOTH = 2;

const OutputOptions = [
    { name: "Output to TRS", value: OUTPUT_TRS },
    { name: "Output to USB", value: OUTPUT_USB },
    { name: "Output to TRS & USB", value: OUTPUT_BOTH },
];

export default OutputOptions;